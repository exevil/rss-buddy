<!DOCTYPE html>
<html>
<head>
    <title>{{ feed.metadata.title }}</title>
    <meta charset="UTF-8">
    <style> /* Add some basic styling */ </style>
</head>
<body>
    <h1>{{ feed.metadata.title }}</h1>
    <p>{{ feed.metadata.description }}</p>
    <ul>
        {% for item in feed.items %}
        <li>
            <a href="{{ item.link }}">{{ item.title }}</a>
            <p>{{ item.description }}</p>
            {% if item.pub_date %} ({{ item.pub_date }}) {% endif %}
            {% if item.tag %} [{{ item.tag }}] {% endif %}
        </li>
        {% else %}
        <li>No items in this feed.</li>
        {% endfor %}
    </ul>
</body>
</html>